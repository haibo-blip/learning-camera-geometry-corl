\paragraph{Shared policy architecture.}
Unless otherwise stated, the RGB observation encoder uses ViT-S scale
components with hidden size 384, 6 transformer layers, 6 attention heads, and
patch size 16.  The action expert is a flow-matching DiT with hidden size 384,
8 layers, and 6 attention heads.  Policies predict 32-step action chunks and
execute actions with an 8-step replanning interval in simulation.

\paragraph{LIBERO-MV Spatial.}
LIBERO-MV experiments follow the setup of Camera-Aware Multi-View Visuomotor
Policies~\citep{anonymous2026cameraaware}: two random surround views
(\texttt{surround2}), no wrist view, \(128{\times}128\) RGB inputs, 100k
training steps, batch size 256, AdamW with learning rate \(10^{-4}\), cosine
schedule, 1k warmup steps, bf16 mixed precision, and EMA 0.9999.  Evaluation
uses 10 tasks and 50 rollouts per task.

\paragraph{MimicGen.}
MimicGen policies use 12 random surround cameras per episode and a fixed-seed
5\% demo holdout.  Rollout evaluation is run every 50k optimization steps under
both \texttt{train\_wide} and \texttt{eval\_tight}, with 40 rollouts for each
camera setting.  The E2E camera-pose policies use 50\% episode-level pose
labels in the reported runs.  The NVS variant uses 64 active scene tokens and
target-observation dropout probability 0.6 during NVS training.  Diffusion
Policy and ManiFlow use their original training recipes as closely as possible;
ManiFlow uses 1024 point-cloud points.

\paragraph{Real-World Tabletop.}
The real-world E2E camera-pose NVS policy uses 50\% episode-level pose labels
and no calibration at inference; the Plucker-GT reference uses full per-episode
calibration at both training and test time.  All conditions use the same robot
platform, camera rig, rubrics, and trial counts; the Plucker-GT and
Plucker-RayZer reference numbers are the calibrated and decoupled baselines
evaluated under the same protocol on the same
hardware~\citep{anonymous2026cameraaware}.  Actions are predicted in the absolute
end-effector frame as 1-second chunks at 50 Hz.

\section{Camera-Pose Loss}

Table~\ref{tab:appendix-cam-pose-loss} reports the camera-pose prediction
metrics for the MimicGen E2E camera-pose NVS runs.  Each value is averaged over
the last 10 logged W\&B points available for that metric.  The supervised pose
loss is computed on pose-labeled episodes, while the visual pose-error metrics
are logged from held-out visualization rollouts.

\begin{table}[ht]
  \centering
  \caption{Camera-pose prediction losses and errors for the MimicGen NVS policy.}
  \label{tab:appendix-cam-pose-loss}
  \resizebox{\linewidth}{!}{
  \begin{tabular}{lrrrrrr}
    \toprule
    Task & Train step & Pose loss & Rot. err. (deg) & Trans. L1 & Vis. rot. err. (deg) & Vis. trans. err. (m) \\
    \midrule
    Stack Three & 145900 & 0.0309 & 1.741 & 0.00094 & 0.197 & 0.00472 \\
    Stack & 400000 & 0.0327 & 1.873 & 0.00007 & 0.108 & 0.00375 \\
    Threading & 483700 & 0.0314 & 1.756 & 0.00151 & 0.216 & 0.00673 \\
    Coffee & 800000 & 0.0330 & 1.884 & 0.00022 & 0.104 & 0.00313 \\
    \bottomrule
  \end{tabular}}
\end{table}

\section{LIBERO-MV Pose Accuracy vs.\ Label Ratio}

Table~\ref{tab:appendix-pose-sweep} reports the converged camera-pose error of
the coupled E2E policy at each label ratio of the LIBERO-MV Spatial sweep,
alongside the decoupled supervised-only pose head, with the corresponding task
success.  Each error is averaged over the last logged evaluation points.  Pose
error falls monotonically with more labels and is lowest for the fully-labeled
coupled run; the decoupled head is more accurate than every partially-labeled
coupled run, but task success is essentially flat across the coupled runs and
lowest for the decoupled head --- metric pose accuracy does not track control
performance.

\begin{table}[ht]
  \centering
  \caption{Converged pose accuracy and Spatial success vs.\ pose-label ratio on
  LIBERO-MV. The decoupled supervised-only pose head is more accurate than the
  partially-labeled coupled runs yet yields the lowest success.}
  \label{tab:appendix-pose-sweep}
  \begin{tabular}{lccc}
    \toprule
    Pose labels & Rot. err.\ (deg)\,$\downarrow$ & Trans. err.\ (m)\,$\downarrow$ & Spatial success\,$\uparrow$ \\
    \midrule
    10\%  & 7.634 & 0.1816 & 0.820 \\
    30\%  & 4.063 & 0.1181 & 0.775 \\
    50\%  & 2.422 & 0.0710 & \textbf{0.865} \\
    80\%  & 1.433 & 0.0537 & 0.780 \\
    100\% & \textbf{0.159} & \textbf{0.0040} & 0.860 \\
    \midrule
    Decoupled (supervised only) & 0.334 & 0.00576 & 0.660 \\
    \bottomrule
  \end{tabular}
\end{table}

\clearpage
\section{Novel-View Synthesis Quality}

Fig.~\ref{fig:appendix-nvs-quality} shows one validation NVS reconstruction
example per MimicGen task.  Each strip contains the remaining source view, the
ground-truth target view, and the predicted target view, after removing the
duplicated target input from the raw W\&B visualization.

To confirm the NVS branch learns a non-trivial scene representation rather than
copying an input view, Table~\ref{tab:appendix-nvs-floor} compares its
reconstructions against a copy-source-view floor (the non-target input view used
directly as the prediction) on the Stack validation splits.  The decoder
improves over the floor on both PSNR and LPIPS under the train-camera and the
tighter held-out camera distribution, so the scene tokens carry view-synthesis
information beyond the nearest available input.

\begin{table}[ht]
  \centering
  \caption{NVS reconstruction quality vs.\ a copy-source-view floor on the Stack
  validation splits (train-camera and tight-camera). Metrics are computed from
  the val NVS visualization panels; the source tile duplicating the target is
  excluded.}
  \label{tab:appendix-nvs-floor}
  \begin{tabular}{lcccc}
    \toprule
    Method & \multicolumn{2}{c}{train-camera} & \multicolumn{2}{c}{tight-camera} \\
    \cmidrule(lr){2-3}\cmidrule(lr){4-5}
           & PSNR\,$\uparrow$ & LPIPS\,$\downarrow$ & PSNR\,$\uparrow$ & LPIPS\,$\downarrow$ \\
    \midrule
    Copy source view    & 9.05  & 0.602 & 12.76 & 0.369 \\
    Ours (NVS decoder)  & \textbf{16.17} & \textbf{0.366} & \textbf{14.98} & \textbf{0.257} \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[ht]
  \centering
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{cc}
    \textbf{Stack Three} & \textbf{Stack} \\
    \includegraphics[width=0.47\linewidth]{figures/appendix/nvs_quality/stack_three_nvs.png} &
    \includegraphics[width=0.47\linewidth]{figures/appendix/nvs_quality/stack_nvs.png} \\
    \textbf{Threading} & \textbf{Coffee} \\
    \includegraphics[width=0.47\linewidth]{figures/appendix/nvs_quality/threading_nvs.png} &
    \includegraphics[width=0.47\linewidth]{figures/appendix/nvs_quality/coffee_nvs.png} \\
  \end{tabular}
  \caption{NVS reconstruction examples from the E2E camera-pose NVS policy. In each strip, columns are source view, ground-truth target view, and predicted target view.}
  \label{fig:appendix-nvs-quality}
\end{figure}
