\subsection{LIBERO-MV Spatial}

The LIBERO-MV Spatial experiment isolates end-to-end camera-pose learning.  We
vary the fraction of training episodes with ground-truth camera-pose labels and
compare against calibrated geometry-aware references.  Fig.~\ref{fig:libero}
shows two trends.  First, a small amount of pose supervision is already useful:
the 10\% setting reaches a success rate close to the calibrated Plucker
baseline.  Second, the best current 50\% setting exceeds the calibrated PRoPE
reference in this sweep, suggesting that action loss can shape the predicted
geometry toward what the policy needs for control.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/libero_mv_spatial_summary.png}
  \caption{LIBERO-MV Spatial summary. Left: calibrated references and the best current E2E camera-pose action policy. Right: episode-level success as the fraction of camera-pose-labeled training episodes varies.}
  \label{fig:libero}
\end{figure}

\subsection{MimicGen Camera-Shift Evaluation}

The MimicGen experiment evaluates episode-level success under two camera
conditions: the train-camera distribution and a tighter held-out camera
distribution.  Fig.~\ref{fig:mimicgen-prelim} replaces the draft table with a
multi-task result figure, which is easier to read in the main paper while the
full run set is still evolving.  Hammer Cleanup is omitted from the current
plot because we do not plan to run it in this round.  The NVS policy improves
over the E2E camera-pose action policy on the completed Stack Three, Coffee,
and Threading runs under both train-camera and tight-camera evaluation.  The
Stack NVS run remains pending, so the plot leaves that cell blank rather than
imputing a value.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/mimicgen_multitask_results.png}
  \caption{Preliminary MimicGen 100-demo results. Bars report episode-level success rates under train-camera and tight-camera evaluation. Diffusion Policy is an image baseline, ManiFlow is a point-cloud baseline, and the two E2E variants match the model names in Fig.~\ref{fig:pipeline}.}
  \label{fig:mimicgen-prelim}
\end{figure}

\subsection{Real-World Tabletop Evaluation}

We are also beginning a real-world tabletop evaluation following the
camera-conditioned tabletop setting of \citet{jiang2025knowyourcamera}.  For a
first comparison, Fig.~\ref{fig:realworld-blocks} uses only their calibrated
Plucker-GT result on Blocks-in-Bowl and compares it with our current E2E
camera-pose NVS policy under the same success definition and 10-trial protocol.
Camera Rays reports 50\% full-task success and 86\% rubric-weighted task
progress for Plucker-GT on this task.  Our current Blocks-in-Bowl run reaches
100\% full-task success.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.68\linewidth]{figures/realworld_blocks_bowl_comparison.png}
  \caption{Initial real-world Blocks-in-Bowl comparison under the same 10-trial success definition. The Camera Rays number is the reported calibrated Plucker-GT result; the outlined extension shows their task-progress score.}
  \label{fig:realworld-blocks}
\end{figure}

\subsection{Planned Baselines and Ablations}

The main completed baseline set compares image Diffusion Policy, point-cloud
ManiFlow, the E2E camera-pose action policy, and the E2E camera-pose NVS policy.
The next ablations should separate the effects of pose-label ratio, action-loss
end-to-end training, NVS regularization, target-view dropout, and the
scene-token bottleneck.
